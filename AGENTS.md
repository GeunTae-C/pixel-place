# pixel-place agent instructions

이 저장소의 구현 기준 문서는 아래 2개다. 코드를 수정하기 전에 먼저 읽을 것.

- docs/pixel-place-specs.md
- docs/pixel-place-details.md

## 단계별 정책 우선순위

- 단계별 명령문은 순차 적용 이력이다.
- 동일 정책에 서로 다른 설명이 있으면 더 높은 단계 번호에서 명시적으로 교정한 정책이 우선한다.
- 현재 활성 flush/checkpoint 정책은 10.6단계와 10.6이 갱신한 `명령문/010.5단계(최종수정).md`, `명령문/_5~20단계_정리260703.txt`를 기준으로 한다.
- 현재 활성 API/profile 정책은 10.7단계를 기준으로 한다.
- `_5~20단계 정리260606.txt`는 과거 이력 문서이며 현재 정책 기준으로 사용하지 않는다.
- 과거 단계의 금지 예시나 제거 대상 문구를 현재 유효 정책으로 오인하지 않는다.

핵심 구현 순서:
1. 부팅/복구 코어
2. write path
3. read path
4. flush worker

중요 불변식:
- 실시간 authoritative state는 DB가 아니라 메모리 타일 상태다.
- WAL append + fsync 성공은 write의 1차 내구성 경계다.
- core write 완료는 WAL append + fsync와 memory apply가 모두 성공한 상태다.
- 현재 HTTP accepted 응답은 core write 완료 후 command 계약상 dirty mark까지 성공한 상태를 뜻한다.
- DB flush 완료는 HTTP 성공 조건이 아니다.
- checkpoint는 DB tiles + pixel_events 모두 flush 완료된 지점이어야 한다.

작업 규칙:
- 한 번에 너무 넓게 수정하지 말고 작은 단위로 진행할 것.
- 기존 문서의 불변식을 깨는 변경은 하지 말 것.
- 새 파일을 만들 때는 패키지 구조를 기능 기준으로 유지할 것.
- 구현 후 변경 이유를 짧게 하지만 빼먹는 것 없이 요약할 것.

## 주석 규칙

- 주석은 한국어로 작성한다.
- 주석은 코드의 동작을 한 줄씩 설명하기보다, 왜 이 클래스/메서드가 존재하는지, 어떤 설계 의도와 불변식/복구 규칙을 지키는지 중심으로 작성한다.
- 클래스 상단에는 파일의 역할과 존재 이유를 설명하는 주석을 추가한다.
- 중요한 public 메서드에는 호출 목적, 기대하는 상태, 지켜야 하는 규칙이 드러나도록 주석을 추가한다.
- 예외를 던지는 분기와 복구 규칙이 걸린 조건문에는, 왜 여기서 실패 처리하는지, 왜 이 조건이 중요한지를 짧게 설명하는 주석을 추가한다.
- stub 구현에는 어떤 profile/테스트 입력을 무엇으로 대체하는지 책임을 명시한다.
- 실제 production adapter가 이미 존재하는 stub을 “실제 구현 전 임시체” 또는 “이후 real 구현으로 교체될 자리”라고 설명하지 않는다.
- JPA 매핑처럼 읽는 사람이 헷갈릴 수 있는 부분에는, 매핑 이유나 주의할 점이 드러나도록 주석을 추가한다.
- 모든 줄에 주석을 달 필요는 없지만, 이해에 중요한 부분은 위치를 따로 지정하지 않아도 판단해서 충분히 주석을 추가한다.

## 주석 양식
- 클래스 상단: 이 클래스의 역할, 존재 이유, 핵심 불변식/복구 규칙
- 중요한 public 메서드: 호출 목적, 기대 결과, 실패가 중요한 이유
- 예외 분기: 왜 여기서 실패 처리하는지 한 줄 설명
- stub 구현: 대체하는 port/input과 활성 profile, 책임 제한을 명시
- JPA 매핑: 왜 이런 매핑 구조를 택했는지 설명

## 테스트 코드 주석

- 테스트 메서드명은 검증 대상과 기대 결과가 드러나도록 작성한다.
- 테스트 코드 주석도 한국어 음슴체, 명사형, 역할 중심 표현을 사용한다.
- 모든 테스트에 주석을 달 필요는 없다.
- 테스트 메서드명만으로 의도가 충분히 드러나는 단순 테스트는 주석을 생략해도 된다.
- 다만 MVP 제한, 예외 매핑, service 호출 금지, recovery/checkpoint 불변식, controller/service 경계처럼 설계 의도를 고정하는 테스트에는 짧은 주석을 남긴다.
- given/when/then 주석은 필요할 때만 사용하며, 기계적으로 모든 테스트에 넣지 않는다.

## 주석 문체

* 주석은 문장형 설명체보다 음슴체, 명사형, 역할 중심 표현을 우선 사용한다.
* `읽는다.`, `분리한다.`, `조회한다.`, `~한다.`, `~다.`처럼 문장을 완성하는 설명체는 가능한 한 피한다.
* 대신 `읽음`, `분리`, `조회`, `검증`, `z=0 tile raw bytes 조회 use case`, `checkpoint 조회와 도메인 변환 경계 담당`처럼 짧고 역할이 바로 드러나는 표현을 사용한다.
* 주석은 코드 동작을 서술하기보다 책임, 경계, 불변식, 복구 규칙, 의도 제한을 음슴체로 남긴다.
* 책임 범위를 제한해야 하는 경우 `대체하면 안 됨`, `책임은 갖지 않음`, `이 구현체가 맡음`, `여기서는 검증만 담당`처럼 명확하게 적는다.
* 복구/초기화/체크포인트처럼 불변식이 중요한 부분은 짧은 문장형보다 이유가 드러나는 음슴체 주석을 우선한다.

예시:

```java
// checkpoint 조회와 도메인 변환 경계를 이 구현체가 맡음

// readiness 상태 전환 책임은 갖지 않음

// 기존 recovery 흐름 대체하면 안 됨

// z=0 tile raw bytes 조회 use case

// recovery와 z=0 전체 pre-init 검증이 같은 숫자를 공유해야 조용한 불일치를 막을 수 있음

// WAL replay 이후 nextEventSeq 계산 기준 보존

// stub profile에서 startup recovery tile snapshot 입력만 결정론적으로 대체
```

피해야 하는 예시:

```java
// checkpoint를 조회하고 도메인 객체로 변환한다.

// 이 클래스는 readiness 상태를 변경하지 않는다.

// recovery와 z=0 전체 pre-init 검증이 같은 숫자를 공유해야 조용한 불일치를 막을 수 있다.
```
